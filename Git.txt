PERDON POR MI FALTA DE ORTOGRAFIA D:
clonar el repositorio dentro de la carpeta que quieras xd
git clone https://github.com/DarkMax27/UNIVERSALBANDS.git
Accede a la carpeta que se creo al clonarse (si te falla ala tratar de subir un cambio puede ser eso)
Antes de hacer esto debes de usar git pull pero ten en cuenta que talvez se borren tus archivos 
aparte debes de hacer esto para no borrar los archivos de otros
mejor avisa en WhatsApp que vas a commitear algo >:v
git remote add origin https://github.com/DarkMax27/UNIVERSALBANDS.git (Solo una vez se usa ya las demás veces no)
para subir usar 
git init (1 necesario)
git add -A (2 necesario)
git status (Solo es para verificar)
git commit -m "Version" (3 necesario)

git pull (acabo de confirmar que si es necesario D: )
git push (5 necesario)

Para meter los cambios hechos por los demás en tu carpeta (actualizar tu carpeta con el repositorio local)
git pull (Ya está)

